import java.util.*;
public class Pattern {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);  // helps for taking user input and store in sc
      
       String name = sc.nextLine();        // next line helps to read the spaces. will print the whole in input
        System.out.println(name);

        

        float price = sc.nextFloat();     // if float then have to type sc.nextFloat
        System.out.println(price);
        sc.close();
        
    }
    
}

